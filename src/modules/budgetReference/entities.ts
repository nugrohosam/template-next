export type BudgetReference = {
  id: string;
  budgetCode: string;
  districtCode: string;
  divisionCode: string;
  departmentCode: string;
  currency: string;
  balance: number;
  currentBalance: number;
};
