export type ExpenseElement = {
  districtCode: string;
  expElement: string;
  expElementDescription: string;
};

export type District = {
  districtCode: string;
};

export type Department = {
  districtCode: string;
  deptCode: string;
  deptNfs: string;
  divisiNfs: string;
};
